# AGENTS.md

## 操作红线

禁止批量删除文件或目录。不要使用：

- `del /s`
- `rd /s`
- `rmdir /s`
- `Remove-Item -Recurse`
- `rm -rf`

需要删除文件时，只能一次删除一个明确路径的文件。正确示例：

```powershell
Remove-Item "C:\path\to\file.txt"
```

如果需要批量删除文件，应停止操作，并请求用户手动删除。

所有带密钥、token、secret、password、API key 的文件都不要提交 git。提交前必须确认隐私文件和配置文件边界。

## 当前产品主题

AlphaAiGraph 是一个 **AI Agent 驱动的渐进式研究图谱产品**。

它不是普通知识图谱工具、PDF 总结器、静态脑图、学习卡片工具或聊天工具。它的核心是：用户输入一个复杂对象，AI Agent 自动分析并生成研究工作流脑图，标注重点、难点、风险点、推荐下钻点和验证点；用户自主选择研究路径，AI 再按节点逐层深入。

复杂对象可以是：

- 项目工程
- 代码仓库
- PDF
- 文档集
- 技术方案
- 知识体系问题

产品第一感应该是：

```text
我把一个复杂东西交给 AlphaAiGraph。
AI Agent 先画出研究地图。
它告诉我哪里重要、哪里难、哪里有风险、哪里值得继续深入。
我选择研究路径。
每一步追问、证据、验证、结论都会沉淀在图上。
```

## 核心循环

AlphaAiGraph 的主流程是：

```text
输入复杂对象
  -> AI Agent 扫描和理解对象
  -> 生成第一层研究 workflow 脑图
  -> 标注重点、难点、风险、推荐下钻点、待验证点
  -> 用户选择某个节点深入
  -> AI 展开下一层节点
  -> 用户继续提问、查看证据、生成脚本、执行验证、记录结论
  -> 结果回填到对应节点
  -> 长期沉淀为类似 Obsidian 的关系图谱
```

重要原则：不要一次性把项目或 PDF 彻底分析完。默认应先生成高层研究地图，再由用户选择下钻方向。AI 是研究向导，不是强制流程控制器。

## 多线程研究

AlphaAiGraph 必须方便在一个项目内做多线程分析。

这里的“多线程”不是简单的系统线程概念，而是产品里的并行研究能力：

- 一个项目里可以同时分析多个文档、PDF、代码文件、命令输出或资料片段。
- 一个项目可以有多个研究会话、多个研究脑图、多个研究分支。
- 一个文档的研究脑图内，可以对多个章节、概念、风险点或验证点并行执行分析。
- 一个节点可以发起一组并行任务，例如总结、概念抽取、证据抽取、命令执行、脚本生成、结果对比。
- 并行任务应能单独排队、运行、失败、取消、重试和产出结果。

并行研究不能污染主线研究上下文。并行分支和并行任务的结果默认应先进入候选区，例如候选节点、候选证据、候选结论、候选执行结果。只有用户确认或明确的合并策略允许时，结果才回填到主研究图。

核心原则：

```text
主线程承载当前研究路径。
研究分支承载并行分析。
并行任务承载具体 Agent 工作或命令执行。
候选结果经过确认后再合并回主图。
关系图谱可以投影这些并行产物，但主图不能被后台任务静默改写。
```

## 两个图面

AlphaAiGraph 有两个相关但不同的图面。

### 研究工作流脑图

这是当前研究会话的主画布，表达“怎么研究这个对象”。

它应该回答：

- 这个项目/PDF 的结构是什么？
- 哪些地方是重点？
- 哪些地方是难点？
- 哪些地方有风险？
- 哪些节点值得继续下钻？
- 哪些问题需要验证？

### Obsidian-style 关系图谱

这是长期沉淀的全局关系网络，表达“对象之间怎么关联”。

它应该回答：

- 项目、文件、模块、章节、概念、问题、证据、结论之间怎么连接？
- 哪些节点是中心节点？
- 哪些证据支撑了哪些结论？
- 哪些问题仍然孤立或未解决？

不要把这两个图混成一个概念。研究工作流脑图表达当前研究过程；Obsidian-style 关系图谱表达长期关系网络。

## AI Agent 职责

AI Agent 不是普通聊天助手。它是研究向导、结构化分析器和验证辅助者。

它应该：

- 自动读取项目、PDF、文档集合
- 生成第一层研究图
- 标注重点、难点、风险点、推荐下钻点、待验证点
- 解释为什么做这些标注
- 根据用户选择展开某个节点
- 回答节点级问题
- 关联证据来源
- 生成验证动作、测试脚本或检查命令
- 在用户允许后记录执行结果
- 将发现、证据、结果、结论回填到图谱
- 维护长期关系图谱

AI 的行为应体现“辅助意识”：

```text
这里可能是核心入口。
这里比较难，建议单独研究。
这里有风险，最好验证。
这里值得下钻。
这里可以暂时跳过。
```

## 用户职责

用户不是从空白脑图开始画，也不是被固定 workflow 推着走。用户在 AI 给出的研究地图上自主选择路径。

用户可以：

- 点击重点节点深入
- 跳过低优先级节点
- 专门研究难点
- 查看证据
- 对某个节点继续提问
- 要求 AI 换角度重画
- 生成测试脚本
- 执行验证
- 将节点标记为已理解、已验证、待处理或暂缓

## 数据模型方向

后续实现应从研究过程对象出发，而不是从旧的知识点图谱出发。

建议核心模型包括：

- `ResearchObject`：用户输入的复杂对象，例如项目、PDF、文档集。
- `ResearchSession`：一次研究会话。
- `ResearchThread`：研究会话内的一条研究线程或分支，用于并行分析。
- `ResearchMap`：当前研究 workflow 脑图。
- `ResearchNode`：研究单元。
- `ResearchEdge`：研究单元之间的关系。
- `SourceAsset`：项目内的文档、PDF、文件、页面、命令输出等可分析资料。
- `AgentMarker`：AI 对节点的辅助判断。
- `EvidenceAnchor`：证据锚点，指向 PDF 页、代码行、文件片段、命令输出等。
- `ResearchAction`：节点上的建议动作。
- `AgentJob`：异步或并行的 Agent 工作单元。
- `ParallelAnalysisGroup`：一组一起发起、一起查看和合并的并行任务。
- `ExecutionRun`：脚本、测试、命令等执行结果。
- `RelationshipGraph`：由研究数据投影出的 Obsidian-style 关系图谱，不作为第二套主数据源。

`ResearchNode.kind` 表示节点是什么，例如：

- `root`
- `module`
- `section`
- `concept`
- `question`
- `hypothesis`
- `finding`
- `risk`
- `action`
- `script`
- `result`
- `conclusion`
- `evidence`

重点、难点、风险、推荐下钻点、待验证点不要做成节点类型，应该做成 `AgentMarker`。一个节点可以同时是模块、重点、难点和推荐下钻点。

`AgentMarker.kind` 可以包括：

- `focus`
- `difficulty`
- `risk`
- `recommended_drilldown`
- `needs_validation`
- `low_priority`
- `uncertain`
- `core_entry`

完整数据模型以 `docs/superpowers/specs/2026-05-09-alpha-ai-graph-agent-research-map.md` 的 `Data Model` 章节为准。实现时不要沿用旧的知识点、学习卡片或静态图谱模型。

## 临时会话与知识整理

研究过程中必须支持 **临时概念会话 / Side Inquiry**。

场景：

```text
用户正在研究主线节点 A。
突然遇到一个不懂的概念 B。
如果直接在主会话里问，会污染主研究上下文，也会打断节奏。
```

因此应支持旁路提问：

```text
选中概念或节点
  -> 打开临时会话
  -> AI 基于当前项目上下文解释
  -> 用户可以连续追问
  -> 默认不改变主研究图
  -> 用户决定是否沉淀
```

临时会话默认必须隔离：

- 不修改主研究图
- 不改变当前节点状态
- 不触发主 workflow 展开
- 不进入主 Agent 研究路径
- 不污染当前研究上下文

只有用户明确选择时，才沉淀为：

- 回填当前节点说明
- 生成新的研究节点
- 保存为项目概念笔记
- 加入 Obsidian-style 关系图谱
- 作为证据或结论的一部分

建议新增模型：

- `SideInquirySession`：临时会话。
- `InquiryMessage`：临时会话消息。
- `ConceptNote`：沉淀后的概念解释或知识笔记。
- `KnowledgeDigest`：当前项目知识整理结果。

`SideInquirySession` 应包含上下文策略：

- `local_node`：只围绕当前节点解释。
- `current_branch`：使用当前研究分支上下文。
- `whole_project`：使用整个项目或 PDF 上下文。

项目知识整理机制应能把临时问答、节点结论、证据和执行结果整理成：

- 关键概念
- 重要模块解释
- 常见疑问
- 已验证结论
- 待进一步研究问题
- 概念之间的关系

核心原则：

```text
主研究图承载研究主线。
临时会话承载旁路理解。
知识整理机制把有价值的旁路内容沉淀回项目知识和全局关系图谱。
```

## UI 方向

主界面应是三栏研究工作台：

```text
左侧：Agent、研究对象、资料列表、研究会话、研究线程、当前路径
中间：研究 workflow 脑图，也就是当前研究地图，需要显示运行中的节点和候选结果
右侧：资料、PDF、代码、证据、临时会话、并行任务队列、执行结果、合并审核
```

另有 Obsidian-style 全局关系图谱视图，可作为中间画布的切换模式或单独视图。

并行研究 UI 必须让用户看清：

- 哪些线程正在跑
- 每个并行任务分析了哪些资料或节点
- 哪些结果只是候选结果
- 哪些结果已经合并进主研究图
- 哪些命令或脚本正在执行、成功、失败或被取消
- 某个结论来自哪个资料、线程、任务、命令和证据

## 与旧方向的区别

旧方向偏向：

```text
资料 -> 知识点 -> 关系 -> 学习卡片 / 综合大纲 / 导出
```

新方向是：

```text
项目/PDF/资料集合
  -> AI Agent 研究 workflow 脑图
  -> 标注重点、难点、风险、推荐下钻点
  -> 用户选择下钻
  -> 分析、提问、临时会话、验证、结论
  -> Obsidian-style 长期关系图谱
```

`Study Mode`、`Synthesis Mode`、`Export Center` 可以保留为辅助能力，但不应作为产品主轴。它们应成为研究节点上的动作或研究图谱的输出能力。


## Spec Workflow

- 当用户说，"启动交互式工作流"时，开始扫描TODO.md内的下一步任务，执行任务，执行完后标记完成。继续执行下一个任务。
- 如果所有的下一步任务都执行完毕，则等待600秒，启动命令行工具，停止思考，再次读取TODO.md查看是否有未完成的下一步任务
- 待确认任务列表不要执行
- TODO.md随时会被用户修改，每次都重新读取。
- 交互式会话只有一种退出条件，就是当TODO.md的里程碑状态被改为已完成
- AGENT只能修改TODO.md的任务完成状态，其他都不能修改
- 不要帮用户建立自动化任务
- 如果程序触发crash，或者无响应，需要关闭程序，选择其他方式。
