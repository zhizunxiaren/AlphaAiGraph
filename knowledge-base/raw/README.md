# Raw Sources

此目录存放用户明确加入项目知识库的原始资料或其稳定引用。原始资料是 source of truth：Agent 可读取、索引和引用，但不修改原文。

每个来源应有稳定 `source_id`。大文件可只保存 URI、hash 和 locator manifest；不要把包含 key、token、secret、password、API key、凭证或私有配置的文件复制到这里。
