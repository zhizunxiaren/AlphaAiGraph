import { researchFixtures } from "../research/fixtures";

export const seedResearchWorkspace = researchFixtures;

export const legacyGraphSeed = {
  nodes: [],
  edges: [],
  compatibility: "legacy graph exports are retained only as projection compatibility",
};
